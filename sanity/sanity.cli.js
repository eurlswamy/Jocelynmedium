import { defineCliConfig } from 'sanity/cli'

export default defineCliConfig({
  api: { projectId: 'ji3lirnr', dataset: 'production' },
  studioHost: 'jocelyn-medium', // -> https://jocelyn-medium.sanity.studio
})
